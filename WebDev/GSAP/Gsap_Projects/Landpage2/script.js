// let tl = gsap.timeline();

gsap.from("nav #logo", {
    y:-60,
    delay:0.2,
    opacity:0,
    duration:1,
    // stagger:0.4
    // scrub: 2
});

gsap.from("nav h3", {
    y:-60,
    delay:0.2,
    opacity:0,
    duration:1,
    // stagger:0.4
    // scrub:2
});